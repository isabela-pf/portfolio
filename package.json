{
    "name": "@zero-trust/docs",
    "private": true,
    "version": "0.0.0",
    "scripts": {
        "setup": "curl -LsSf https://astral.sh/uv/install.sh | sh && uv venv && uv sync",
        "dev": "uv run mkdocs serve --dev-addr=127.0.0.1:8001"
    }
}
